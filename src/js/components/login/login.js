export default React => () => {
  return (
    <div className="jptv-login">
      <h2>Login</h2>
      <label>Username / email:</label>
      <input type="text" />
      <label>Password:</label>
      <input type="password" />
    </div>
  );
}