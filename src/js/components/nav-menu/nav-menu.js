import { Link } from 'react-router-dom';

import './nav-menu.css';

export default React => () => {
  return (
    <nav>
      <ul className="jptv-nav-menu">
        <li>Lessons</li>
      </ul>
    </nav>
  );
}