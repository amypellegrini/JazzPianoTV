define([], function() {
  /**
   * Exercise Controller Class definition.
   * Searches the entire App context for exercises and proceeds
   * to initialize them.
   */
  var ExerciseController = function() {
    return {};
  };

  return ExerciseController;
});
